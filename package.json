cat > package.json << EOL
{
  "name": "apex-collector",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@supabase/ssr": "0.1.0",
    "@supabase/supabase-js": "2.39.8",
    "next": "14.1.3",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@types/node": "20.11.26",
    "@types/react": "18.2.65",
    "@types/react-dom": "18.2.21",
    "autoprefixer": "10.4.18",
    "eslint": "8.57.0",
    "eslint-config-next": "14.1.3",
    "postcss": "8.4.35",
    "tailwindcss": "3.4.1",
    "typescript": "5.4.2"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
EOL

# Nettoyer l'installation
rm -rf node_modules
rm -rf .next
npm cache clean --force

# Réinstaller les dépendances
npm install

# Tenter un build local pour vérifier que tout fonctionne
npm run build

# Si le build local fonctionne, redéployer sur Vercel
vercel --prod
